name: ltogt_widgets
description: Brick Design System.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  ltogt_utils:
    path: /home/omni/repos/package/ltogt_utils
  ltogt_utils_flutter:
    path: /home/omni/repos/package/ltogt_utils_flutter
    

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  
flutter:
